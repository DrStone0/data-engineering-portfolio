import logging

logging.basicConfig(
    level=logging.INFO,
    format="%(asctime)s - %(levelname)s - %(message)s"
)

def main():
    logging.info("Data Engineering journey started.")
    logging.info("Virtual environment is working correctly.")

if __name__ == "__main__":
    main()
